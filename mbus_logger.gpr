-- Florist is not needed anymore
-- with "florist";
project Mbus_Logger is

   for Main use ("mbus_logger");
   for Source_Dirs use ("src/");
   for Object_Dir use "build";
   for Exec_Dir use ".";


   package Builder is
      for Default_Switches ("Ada") use ("-g");
   end Builder;

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;

   package Binder is
      for Default_Switches ("Ada") use ("-g");
   end Binder;

end Mbus_Logger;

